---
const url = 'https://api.twitch.tv/helix/streams'
const CLIENT_ID = 'y1b3vqct87lqhyab64i28gy4qfaa35'
const TOKEN_API ='qnrw7pe00dlfxq1xf2uz87c0ktljya'
const respuesta = await fetch(url,{
  method: 'GET',
        headers: {
          'Authorization': `Bearer ${TOKEN_API}`,
          'Client-Id': CLIENT_ID
        }
})
const info = await respuesta.json()
console.log(info)
const streamer = info.data[Math.floor(Math.random( )* info.data.length)]
---
<div id="recomended" class=" border-2 border-black  h-screen text-xs flex flex-col  items-center w-full"> 
  <h2 class=" font-semibold m-2">RECOMENDADED CHANNELS</h2>
  <p>{streamer.game_id}</p>
  <p>{streamer.title}</p>

  <!-- iteramos con un map  -->

</div>