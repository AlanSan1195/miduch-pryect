---
// import background from "../assets/background.svg";
import { Header } from "../components/Header";
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />


    <title>Suitch</title>
  </head>
  <body>
    <Header client:visible />
    <!-- <img id="background" src={background.src} alt="" fetchpriority="high" /> -->
    <slot />
  </body>
</html>

<style>

   :root{

    --color_primary: #161616;
    --color-secundary: #ec4899;
    --color-terciary: #1C1C1C
   }

  #container {
    font-family: Inter, Roboto, "Helvetica Neue", "Arial Nova", "Nimbus Sans",
      Arial, sans-serif;
    height: 100%;
  }
  /* // puse un paddding-top de 60px por qu ela cague nunca hice el HEADER EN FIXED  */
  html{
    color: white;
    background: var(--color_primary);
    padding-top: 60px;
  }
  @utility mask-fade-buttom () {
    mask-image: linear-gradient(180deg, rgba(0, 0, 0, 0.3),);
  }
</style>
