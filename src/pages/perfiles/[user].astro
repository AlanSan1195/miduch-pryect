---

import Layout from "../../layouts/Layout.astro";
import { PerfilUser } from "../../components/Perfiles";
import { RecommendedChannels } from "../../components/RecommendedChannels";
import { AppWraperPerfil } from "../../components/AppWraper";
export const prerender = false;
const { user } = Astro.params;
if (!user) {
  return redirect("/"); // Redirige a la página principal u otra página válida
}
---

<!-- <Layout>
  <RecommendedChannels client:visible />
  <PerfilUser client:only="react" user={user} />
</Layout> -->

<Layout>
  <AppWraperPerfil client:visible user={user} />
</Layout>


