---
import { Header } from "../../components/Header";
import Layout from "../../layouts/Layout.astro";
import { PerfilUser } from "../../components/Perfiles";
import { RecommendedChannels } from "../../components/RecommendedChannels";
export const prerender = false;
const { user } = Astro.params;
---

<Layout>
  <RecommendedChannels client:visible />
  <PerfilUser client:only="react" user={user} />
</Layout>

<!-- experimetal con el de perfils despues del cambio de createContext -->

<!-- <Layout>
  <Header client:visible />
  <AppWraperPerfil client:only="react" user={user} />
</Layout> -->
